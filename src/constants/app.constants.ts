import { env } from '@/config/env';

export const APP_TITLE = 'Project Structure';
export const APP_VERSION = env.appVersion;
export const DEFAULT_PAGE_SIZE = 10;
export const DEFAULT_TIMEOUT_MS = env.requestTimeout;
